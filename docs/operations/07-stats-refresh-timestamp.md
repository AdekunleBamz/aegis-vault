# Stats refresh timestamp

Keep stats refresh timestamps in dashboard QA so users can see whether vault totals are current or waiting on API refresh.

## Checklist

- Throttle the stats endpoint and watch the dashboard state.
- Confirm the last refreshed timestamp is visible near aggregate totals.
