# Frontend env contract sync

Keep frontend environment contract values in release checks so deployed contract names and UI explorer links stay in sync.

## Checklist

- Compare sanitized frontend environment values with the deployment record.
- Confirm stale preview builds are rebuilt after contract-address updates.
