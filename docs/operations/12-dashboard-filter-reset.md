# Dashboard filter reset

Keep dashboard filter reset behavior in QA so users can recover quickly from searches that hide every vault.

## Checklist

- Apply a search or status filter with no matches.
- Confirm the reset action clears all filters and restores the vault list.
