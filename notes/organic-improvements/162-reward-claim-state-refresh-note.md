# Reward claim state refresh note

After a reward claim, verify pending rewards, total claimed, and position status refresh from the same network.

If any value is cached, record the refresh trigger used during smoke testing.

This keeps stale claim displays from being mistaken for failed transactions.
