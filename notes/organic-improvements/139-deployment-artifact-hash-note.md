# Deployment artifact hash

Record the frontend deployment id or artifact hash so rollback verification can
target the intended build.

Also include the source commit in the release note when artifact names are reused.
