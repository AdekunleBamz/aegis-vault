# Contract check warning capture note

Keep the `clarinet check` warning count with release evidence when contract files change.

If warnings are expected, identify whether they come from token, treasury, or vault contracts.

This makes new warnings easier to separate from known static-analysis noise.
