# Frontend build evidence note

Release handoffs should include the `next build` route summary when frontend pages change.

If the build skips type validation, record the separate type-check or lint command used for coverage.

This makes Vercel readiness evidence easier to audit after a deploy.
