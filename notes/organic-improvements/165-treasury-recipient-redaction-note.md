# Treasury recipient redaction note

Treasury support logs should show recipient address prefixes unless the full address is required for transaction replay.

Never include private operator notes, wallet seeds, or local `.env` values in treasury screenshots.

Attach the explorer transaction link when a full address needs verification.
