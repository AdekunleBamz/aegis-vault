# Wallet network preflight note

Before staking smoke tests, confirm the wallet network matches the frontend RPC target and contract address set.

Capture the network badge state before submitting the first transaction.

This catches misconfigured previews before a signer prompt appears.
