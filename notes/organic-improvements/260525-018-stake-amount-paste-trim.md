# Stake amount paste trim

Document a validation note for trimming pasted stake amounts before decimal validation.

The UI should reject hidden characters with a clear message if trimming is not enough.
