# Stake limit copy parity

When stake limits change, update contract comments, UI helper text, and release
notes together so users see one consistent cap.

During review, compare the cap copy against the value used in the staking validator.
