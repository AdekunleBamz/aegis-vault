# Withdrawal queue duplicate request

Track UX copy for duplicate withdrawal requests while an existing queue entry is pending.

The copy should point to the existing request instead of treating the action as a new error.
