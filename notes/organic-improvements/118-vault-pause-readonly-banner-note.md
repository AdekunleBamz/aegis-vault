# Vault pause readonly banner

When writes are paused, keep readonly position views available and label the
pause separately from wallet disconnection.

The banner should not hide claimable balance reads that remain available.
