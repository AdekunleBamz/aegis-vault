# Network Mismatch Toast Order

- Show network-mismatch warnings before balance-loading placeholders.
- Keep the call-to-action button visible while the wallet is reconnecting.
- This avoids users waiting on stale balance fetches for the wrong network.

- Include this checkpoint in the final release handoff review.
