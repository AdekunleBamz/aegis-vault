# Stake form max balance source

Add a UI note for labeling whether the stake form max value comes from wallet balance, cached reads, or fallback data.

The source label helps users understand why the max amount may change after refresh.
