# Release build warning owner

Add a release process note for assigning an owner to any frontend build warning that remains at release time.

Warnings should be acknowledged with context instead of silently accepted.
