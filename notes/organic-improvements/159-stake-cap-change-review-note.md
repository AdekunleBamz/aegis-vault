# Stake cap change review note

Any increase to per-user stake list capacity needs an explicit contract review entry.

Capture the old cap, new cap, affected maps, and frontend pagination assumptions before release approval.

This prevents a storage-limit change from being treated as a cosmetic parameter update.
